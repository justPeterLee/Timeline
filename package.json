{
  "name": "timeline",
  "version": "1.0.0",
  "description": "Timeline",
  "main": "index.js",
  "scripts": {
    "build": "concurrently \"cd ./server && npm run server \" \"cd ./client && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2"
  },
  "devDependencies": {
    "@types/node": "^20.11.16"
  }
}
